import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.popflixapp',
  appName: 'popflix ',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
